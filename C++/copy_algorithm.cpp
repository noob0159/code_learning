#include <iostream>
#include <algorithm>
#include <cmath>
#include <vector>
#include <numeric>

using namespace std;

/*
常见的拷贝和替换算法：
copy(v1.begin(),v1.end(),v.begin());
replace(v.begin(),v.end(),OLDvalue,NEWvalue);
replace_if(v.begin(),v.end(),_verb,NEWvalue);
swap(container c1, container c2);

常见的数学算法：
来自numeric
accumulate(v.begin(),v.end(),value);    value是起始值
fill(v.begin(),v.end(),value); 填充value进v

集合算法：
set_intersection(v1.begin(),v1.end(),v2.begin(),v2.end(),v.begin()) 交集
set_union(v1.begin(),v1.end(),v2.begin(),v2.end(),v.begin())        合集
set_difference(v1.begin(),v1.end(),v2.begin(),v2.end(),v.begin())   并集
*/

int main(){

    system("pause");
    return 0;
}